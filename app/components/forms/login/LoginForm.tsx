import './LoginForm.styles.css';

import {Card, Grid} from '@mui/material'

function LoginForm({}) {

    return <>
        <Card id="login-form-container">
            <Grid container xs={12}>
                Login
            </Grid>
        </Card>
    </>
}
export default LoginForm